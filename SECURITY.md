# Security policy

Use GitHub private vulnerability reporting for security issues involving this site or its published workflow configuration.

Do not place credentials, webhook payloads, account identifiers, private repository names, user media, license tokens, signed URLs, or exploit details in a public issue.

The production media service and paid plugin are not publicly available. Reports about the separate protected plugin must use its publisher-approved private support channel when that channel is announced.
